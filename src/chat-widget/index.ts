/**
 * Chat widget entry — side-effect mount when the lazy chunk loads.
 * Also exported for tests / manual init.
 */
import { mountChatWidget } from './widget';

export { mountChatWidget };

mountChatWidget();
